import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionicTest-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
